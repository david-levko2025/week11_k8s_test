pod is enviroment that runs containers and if there have a lot of trafics and the pod can't handle it
I can run another pod with the same container something like that
so the idea of we can take the image we create and work it in kubernetes