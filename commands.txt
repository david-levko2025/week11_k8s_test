echo "# week11_k8s_test" >> README.md         
git init                                                            
git add README.md                                                   
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/david-levko2025/week11_k8s_test.git
git push -u origin main
git checkout -b dockerfile
git add . 
git commit -m "Dockerfile"
git add . 
git commit -m "Dockerfile finish and .md" 
git push -u origin dockerfile 
git checkout -b pod-yaml  
git add . 
git commit -m "the pod.yaml and the md file" 
git push -u origin pod-yaml
git checkout -b service-yaml
git add . 
git commit -m "the service.yaml and the md file" 
git push -u origin service-yaml
git checkout -b csreenshot
git add . 
git commit -m "the screenshot and the md file" 
git push -u origin sreenshot


docker build -t streamlit .
docker run -d --name streamlitTest streamlit:latest
streamlit run your_app.py --server.port 8080 --server.address=0.0.0.0
kubectl apply -f service.yaml
kubectl apply -f pod.yaml
docker tag streamlit:latest davidlevko2025/streamlit:latest
docker push davidlevko2025/streamlit:latest
kubectl get services
kubectl get pods
